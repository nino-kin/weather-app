name: Build

on:
  push:
    branches: [ master, main ]
  pull_request:

jobs:
  build:
    runs-on: [ ubuntu-20.04 ]
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Docker
        shell: bash
        run: |
          mkdir -p ${{ github.workspace }}/build
          ./scripts/docker.sh setup

      - name: Build
        shell: bash
        run: |
          ./scripts/docker.sh build

      - name: Cleanup
        shell: bash
        run: |
          ./scripts/docker.sh remove-image
